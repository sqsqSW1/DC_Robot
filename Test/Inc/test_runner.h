/**
 ******************************************************************************
 * @file    test_runner.h
 * @brief   测试调度器 — 统一入口, 根据 test_config.h 开关调度各模块测试
 * @note    所有测试调用集中在此, main.c 只需一行 Test_Runner()
 *          新增测试模块时:
 *            1. test_config.h 中添加 TEST_XXX_ENABLE 宏
 *            2. 下方 #if 块中追加对应模块的 _Test() 调用
 ******************************************************************************
 */

#ifndef __TEST_RUNNER_H__
#define __TEST_RUNNER_H__

#ifdef __cplusplus
extern "C" {
#endif

/* Includes ------------------------------------------------------------------*/
#include "test_config.h"

/* ======================================================================== */
/*                         测试调度入口                                       */
/* ======================================================================== */

/**
 * @brief 测试总入口 — 根据 test_config.h 中的开关决定执行哪些测试
 * @note  应在 main() 中 Motor_Init() 之后、while(1) 之前调用一次即可
 *        各模块测试函数内部自带超时保护, 不会永久阻塞
 */
void Test_Runner(void);

#ifdef __cplusplus
}
#endif

#endif /* __TEST_RUNNER_H__ */
